# Pulse Log 개발 TODO

## 1. 목적

현재 구현 상태를 기준으로 남은 작업과 보완 포인트를 정리한다.

## 2. P0 - 안정화

- 실제 기기에서 저장/수정 주요 흐름 재검증
- 입력, 캘린더, 그래프 탭 전환 재검증
- 헤더 톱니바퀴 설정 진입/복귀 흐름 검증
- 그래프 날짜 선택 후 캘린더 이동 흐름 검증
- 캘린더 월력 카드 상단 선택 날짜 요약과 입력 탭 이동 버튼 검증
- 입력 탭 삭제 확인 다이얼로그와 삭제 후 반영 검증
- 화면 회전과 프로세스 재생성 상태 확인
- 키보드 동작 기기별 점검

완료 조건:

- 입력, 캘린더, 그래프 탭 전환 및 설정 진입 시 크래시 없음
- 저장 버튼 누름 후 키보드가 정상적으로 내려감
- 입력란과 저장/삭제 버튼이 같은 행에서 정렬되어 보임
- 삭제 확인 후 입력 필드, 캘린더 상태, 그래프가 즉시 갱신됨
- CSV/PDF 공유 버튼이 Android 공유 시트를 열고 파일을 전달함
- 그래프에서 선택한 날짜가 캘린더 탭에 정확히 반영됨
- 키보드 표시 중 레이아웃이 과도하게 깨지지 않음

## 3. P1 - 알림 기능 완성

- 알림 시간 입력 UX 개선
- 아침/저녁 미기록 판정 로직 정리
- `AlarmManager` 또는 `WorkManager` 기반 실제 스케줄링 구현
- 날짜 변경 시 알림 상태 재계산

완료 조건:

- 설정 저장뿐 아니라 실제 알림이 동작함
- 잘못된 시간 입력은 저장 차단 또는 수정 유도

## 4. P1 - 데이터 안정성

- Room 마이그레이션 전략 수립
- destructive migration 제거
- 기존 사용자 데이터 유지 시나리오 테스트
- 현재 UI에서 사용하지 않는 `daily_notes` 테이블의 유지/제거 방침 확정

완료 조건:

- 스키마 변경 후에도 기존 데이터 유지 가능

## 5. P1 - 테스트 하네스

- ViewModel 저장/수정 테스트
- ViewModel 삭제 테스트
- 날짜 상태 계산 테스트
- 그래프 정책 테스트 확장
- 설정 저장 테스트
- Compose UI 테스트 태그 추가
- 캘린더/그래프 이동 UI 테스트

## 6. P2 - 그래프 개선

현재 완료:

- x축/y축 값 표시
- 범례 추가
- 포인트 표시
- 날짜 열 선택
- 선택 날짜 카드
- 혈압 기준선
- 캘린더 이동 액션
- 미기록 구간 점선 연결과 안내 문구

추가 후보:

- 선택 카드 접근성 개선
- 그래프 터치 영역 테스트
- 체중 그래프 y축 포맷 세분화

## 7. P2 - 브랜딩 정리

현재 완료:

- 런처 앱명 `Pulse Log`
- 하트 로고 기반 런처 아이콘
- 내부 헤더명과 외부 앱명 일치화

현재 완료:

- Play Store 등록용 고해상도 아이콘/그래픽 에셋 생성

추가 후보:

- 스토어 스크린샷 정리

## 8. P2 - 내보내기 개선

현재 완료:

- 최근 30일 / 전체 기록 CSV 공유
- UTF-8 BOM 포함 CSV 생성
- 최근 30일 / 전체 기록 PDF 요약본 공유
- PDF 요약본 최근 추이 그래프 포함
- FileProvider 기반 공유

추가 후보:

- 병원별 제출 양식 고도화
- 기간 직접 선택
- CSV 내보내기 UI 테스트

## 9. P3 - 문서 운영

- 기능 변경 시 README와 docs 동시 업데이트
- communication-bridge 기반 변경 기록 축적
- 릴리즈 전 체크리스트 문서 추가
